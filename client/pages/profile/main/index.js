import React, { Fragment } from 'react';
import ProfileMainSettingsHelper from "../../../components/profile/main/index.js";

const ProfileMainSettingsPage = (props) => {
    return (
        <Fragment>
            <ProfileMainSettingsHelper props={props} />
        </Fragment>
    );
}
export default ProfileMainSettingsPage;